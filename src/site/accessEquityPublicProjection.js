// This file is deliberately public-safe. Records are added only after the
// server-side dual gate has produced a public projection. Keep private and
// owner-review candidates in Palantír artifacts, never here.
export const ACCESS_EQUITY_PUBLIC_PROJECTION = Object.freeze({
  schema_version: "miller-north-access-equity-public-projection-v1",
  findings: Object.freeze([]),
  suggested_follow_ups: Object.freeze([]),
})
