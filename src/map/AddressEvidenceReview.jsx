import { useCallback, useEffect, useMemo, useState } from "react"
import { adminFetch } from "../adminApi.js"

const labels = { already_public_mapped: "Already public", high_confidence_candidate: "Very strong candidates", likely_candidate_needing_review: "Strong candidates", probable_fixed_address_candidate: "Fixed-location research leads", geocoder_valid_occupancy_unverified: "Exact geocode, occupancy unverified", shared_building: "Shared buildings", conflicting_addresses: "Conflicts", approximate_geocode: "Approximate geocodes", partial_address: "Partial addresses", no_address_discovered: "No address found", insufficient_information: "Insufficient information", confidential_private: "Confidential / private", virtual_mobile_service_area: "Virtual / mobile / service area" }
const readable = (value) => String(value || "unknown").replaceAll("_", " ")

export default function AddressEvidenceReview() {
  const [report, setReport] = useState({ records: [], counts: {} }), [category, setCategory] = useState("all"), [query, setQuery] = useState(""), [community, setCommunity] = useState(""), [confidence, setConfidence] = useState(""), [page, setPage] = useState(1), [status, setStatus] = useState("Loading address resolution…")
  const load = useCallback(async () => { const response = await adminFetch("/api/admin/address-resolution"); const body = await response.json().catch(() => ({})); if (!response.ok) return setStatus(body.error || "Address resolution could not be loaded."); setReport(body); setStatus(`${body.meaningfully_evaluated} of ${body.total_active} active resources meaningfully triaged (${body.coverage_percentage}%). ${body.realistically_mappable_if_approved} strong candidates await review; no location was published.`) }, [])
  useEffect(() => { const timer = window.setTimeout(load, 0); return () => window.clearTimeout(timer) }, [load])
  const communities = useMemo(() => [...new Set(report.records.map((item) => item.community || item.municipality).filter(Boolean))].sort(), [report])
  const filtered = useMemo(() => report.records.filter((item) => (category === "all" || item.category === category) && (!query || `${item.resource_name} ${item.service_type || item.facility_type}`.toLowerCase().includes(query.toLowerCase())) && (!community || (item.community || item.municipality) === community) && (!confidence || item.confidence === confidence)), [report, category, query, community, confidence])
  const pageSize = 25, pageCount = Math.max(1, Math.ceil(filtered.length / pageSize)), visible = filtered.slice((page - 1) * pageSize, page * pageSize)
  const summary = [{ id: "all", label: "Active directory", count: report.total_active }, ...Object.entries(labels).map(([id, label]) => ({ id, label, count: report.counts?.[id] || 0 }))]
  return <section className="address-evidence address-resolution" aria-labelledby="address-evidence-title"><header><p className="eyebrow">Administrator only · evidence and private preview</p><h2 id="address-evidence-title">Address Resolution</h2><p role="status">{status}</p><p><strong>Directory:</strong> {report.total_active || 0} active resources · <strong>public map:</strong> {report.counts?.already_public_mapped || 0} reviewed locations · <strong>meaningful coverage:</strong> {report.coverage_percentage || 0}%</p></header><div className="resolution-summary">{summary.map((item) => <button type="button" key={item.id} aria-pressed={category === item.id} onClick={() => { setCategory(item.id); setPage(1) }}><strong>{item.count || 0}</strong><span>{item.label}</span></button>)}</div><div className="resolution-filters" aria-label="Address-resolution filters"><label>Search<input value={query} onChange={(event) => { setQuery(event.target.value); setPage(1) }} placeholder="Program or category"/></label><label>Community<select value={community} onChange={(event) => { setCommunity(event.target.value); setPage(1) }}><option value="">All</option>{communities.map((item) => <option key={item}>{item}</option>)}</select></label><label>Confidence<select value={confidence} onChange={(event) => { setConfidence(event.target.value); setPage(1) }}><option value="">All</option>{["very_strong", "strong", "needs_review", "insufficient", "excluded"].map((item) => <option key={item} value={item}>{readable(item)}</option>)}</select></label></div><p>{filtered.length} results · page {page} of {pageCount}. Coordinates remain administrator-only and are never returned by public map APIs.</p><div className="evidence-cards">{visible.map((item) => <article key={item.canonical_uuid} className={`evidence-card confidence-${item.confidence}`}><div><h3>{item.resource_name}</h3><p><strong>{readable(item.confidence)}</strong> · {readable(item.category)} · {readable(item.recommendation)}</p><dl><dt>Current evidence address</dt><dd>{item.proposed_address || item.current_address || "None"}</dd><dt>BC standardized address</dt><dd>{item.standardized_address || "Not geocoded"}</dd><dt>Community</dt><dd>{item.community || item.municipality || "Unknown"}</dd><dt>Geocoder quality</dt><dd>{item.geocoder ? `${item.geocoder.precision} · score ${item.geocoder.score} · ${item.geocoder.descriptor}` : "No result"}</dd><dt>Occupancy evidence</dt><dd>{item.occupancy_claim?.research_summary || item.old_evidence?.address_evidence_excerpt || "Not established"}</dd><dt>Reason codes</dt><dd>{(item.concerns || item.reason_codes || []).map(readable).join(", ")}</dd><dt>Evidence state</dt><dd>{item.evidence_reusable ? "Reused unchanged evidence" : item.needs_reevaluation ? "Canonical evidence changed; reevaluate" : "Current deterministic triage"}</dd><dt>Publication</dt><dd>{item.public_map ? "Already reviewed and public" : "Not public from this workflow"}</dd></dl>{item.source_url ? <a className="long-url" href={item.source_url} target="_blank" rel="noreferrer">Open occupancy source</a> : null}<details><summary>Technical evidence</summary><pre>{JSON.stringify({ geocoder: item.geocoder, occupancy_evidence: item.occupancy_evidence, changed: item.changed, fingerprint: item.evidence_fingerprint }, null, 2)}</pre></details><div className="resolution-actions"><button type="button" disabled>Confirm address, do not publish</button><button type="button" disabled>Needs more research</button><button type="button" disabled>Reject proposed address</button><small>Durable QC actions remain unavailable until the reviewed QC migration is explicitly authorized.</small></div></div></article>)}</div><nav className="resolution-pagination" aria-label="Address resolution pages"><button type="button" disabled={page <= 1} onClick={() => setPage((value) => value - 1)}>Previous</button><span>Page {page} of {pageCount}</span><button type="button" disabled={page >= pageCount} onClick={() => setPage((value) => value + 1)}>Next</button></nav></section>
}
