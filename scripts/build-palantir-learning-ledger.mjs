import { mkdirSync, writeFileSync } from "node:fs"
import path from "node:path"

import { buildPalantirLearningLedger, createPalantirLearningLesson, proposePalantirRulePromotion } from "../server/palantirOperationalLearning.js"

const root = path.resolve(new URL("..", import.meta.url).pathname)
const at = new Date("2026-09-08T06:10:00.000Z")
const lessons = [
  createPalantirLearningLesson({ lesson_type: "document_structure", source_id: "research:alberta_ombudsman_case_summaries", observation: "Case-summary pages can contain a complete bounded investigation account even when they do not expose a conventional decision citation.", examples: [{ document_id: "ab-ombudsman-applying-all-relevant-considerations", evidence: "Page records complaint, analysis, recommendations, acceptance and known case-level outcome." }], applicability: "source_specific" }, { now: at }),
  createPalantirLearningLesson({ lesson_type: "parser_rule", source_id: "research:alberta_ohs_convictions", observation: "One annual page represents many distinct enforcement records; the shared page URL is not a duplicate key.", examples: [{ document_id: "ab-ohs-2026-marigold", evidence: "Distinct convicted entity and conviction date on the shared annual page." }, { document_id: "ab-ohs-2025-georges-farm-centre", evidence: "Different entity, date and sentence on the same page." }], applicability: "shared", proposed_rule: "Fingerprint structured enforcement rows from source ID, entity, decision date and outcome rather than source URL alone." }, { now: at }),
  createPalantirLearningLesson({ lesson_type: "document_role", source_id: "research:canadian_transportation_agency_compliance", observation: "A current enforcement index and an annual aggregate report are related but non-interchangeable document roles.", examples: [{ document_id: "cta-compliance-enforcement-current", evidence: "Current index lists notices and links to inquiries." }, { document_id: "cta-annual-report-2024-2025", evidence: "Annual report gives aggregate dispute and enforcement counts." }], applicability: "shared", proposed_rule: "Preserve index/current-status and annual-outcome documents as separate roles and reconcile them under one monitored institution." }, { now: at }),
  createPalantirLearningLesson({ lesson_type: "false_positive", source_id: "shared:resource_url_health", observation: "A HEAD failure is not sufficient evidence that a public program is closed when a bounded GET request can still resolve.", examples: [{ source_id: "shared:resource_url_health", document_id: "url-health-fallback-1", evidence: "Existing health adapter retries 403, 404 and 405 HEAD responses with GET." }, { source_id: "shared:resource_url_health", document_id: "url-health-fallback-2", evidence: "Closure candidacy already requires a repeated failure streak rather than one response." }], applicability: "shared", proposed_rule: "Use bounded GET fallback and repeated-failure evidence before creating a closure candidate." }, { now: at }),
  createPalantirLearningLesson({ lesson_type: "parser_rule", source_id: "research:worksafebc_incident_findings", observation: "WorkSafeBC redacted incident summaries expose a notice-of-incident number even when employers are named only by business class.", examples: [{ document_id: "worksafebc-ni-2023197990005", evidence: "The municipal-roadway summary exposes NI 2023197990005." }, { document_id: "worksafebc-ni-2023194040008", evidence: "The school-construction summary exposes NI 2023194040008." }], applicability: "shared", proposed_rule: "Use a regulator-issued notice or case number as the primary document key and retain redacted employer labels without attempting identity inference." }, { now: at }),
  createPalantirLearningLesson({ lesson_type: "document_role", source_id: "research:worksafebc_penalty_summaries", observation: "WorkSafeBC warns that a posted penalty amount and status may change while a review is underway.", examples: [{ document_id: "worksafebc-penalty-index-current", evidence: "The official index states that posted amounts may not be final." }], applicability: "source_specific" }, { now: at }),
  createPalantirLearningLesson({ lesson_type: "parser_rule", source_id: "research:saskatchewan_ohs_prosecutions", observation: "Saskatchewan fiscal-year prosecution tables lead to exact releases that preserve plea, fine, surcharge, and withdrawn-charge roles.", examples: [{ document_id: "sk-ohs-sha-2024-12-11", evidence: "The release separates the guilty plea, $75,000 total, and withdrawn charge." }, { document_id: "sk-ohs-kindersley-2024-08-13", evidence: "The release separates the guilty plea, $175,000 total, and four withdrawn charges." }], applicability: "shared", proposed_rule: "Keep guilty pleas, convictions, fines, surcharges, acquittals, stays, and withdrawn charges as separate structured procedural fields." }, { now: at }),
  createPalantirLearningLesson({ lesson_type: "document_role", source_id: "human_rights_intersection", observation: "Workplace discrimination material can be a merits finding, a procedural referral, or a mediated resolution; those roles are not interchangeable.", examples: [{ document_id: "2008-BCHRT-104-official-digest", evidence: "The official digest records a merits finding and remedy." }, { document_id: "2021-AHRC-95", evidence: "The official summary records referral to hearing and later settlement, not a merits finding." }, { document_id: "sk-shrc-annual-2023-24-caitlin", evidence: "The annual report records mediation, not adjudication." }], applicability: "shared", proposed_rule: "Classify human-rights merits, procedural referral, and mediated settlement separately before cross-domain routing." }, { now: at }),
  createPalantirLearningLesson({ lesson_type: "access_blocker", source_id: "research:saskatchewan_wcb_appeal_decisions", observation: "The publication policy says reasons apply to appeals submitted on or after October 1, 2024, but the policy page was intermittently unavailable during this cycle.", examples: [{ document_id: "sk-wcb-pro-14-2024", evidence: "Search discovery returned the policy metadata while direct retrieval returned a transient gateway error." }], applicability: "source_specific" }, { now: at }),
  createPalantirLearningLesson({ lesson_type: "document_role", source_id: "shared:resource_reconciliation", observation: "A public-record report can reveal a useful service, but the report is not the canonical resource; the service must be verified against a current first-party service page before routing.", examples: [{ document_id: "resource-reconciliation-2026-09-08", evidence: "Benefits, corrections, legal-support and mobile-gap research produced separately verified service records while evidence and accountability documents remained excluded." }, { document_id: "resource-reconciliation-private-review-2026-09-08", evidence: "Candidates without a working first-party intake or service page remained in private review." }], applicability: "shared", proposed_rule: "Route only the separately verified practical service or program into canonical resource projections; never promote its discovery document as the resource." }, { now: at }),
  createPalantirLearningLesson({ lesson_type: "parser_rule", source_id: "shared:resource_reconciliation", observation: "A newly verified canonical record may correspond to a long-lived legacy search record and must enrich that stable identity instead of creating a second result.", examples: [{ document_id: "curated:s68huf", evidence: "Fraser Health access-line verification retained the stable legacy ID while adding current regional scope and contact metadata." }, { document_id: "curated:1wfj0t6", evidence: "BC Housing supportive-housing verification retained the stable legacy ID while replacing stale contact metadata." }], applicability: "shared", proposed_rule: "When a verified canonical ID matches a legacy public record, preserve one identity and let current canonical contact, source, scope and access fields override stale legacy values." }, { now: at }),
  createPalantirLearningLesson({ lesson_type: "parser_rule", source_id: "shared:resource_geography", observation: "A searched community, a service's physical location and its eligible service area are separate facts; regional intake must not be rendered as a local facility.", examples: [{ document_id: "fraser-creekside-regional-pathway", evidence: "Creekside is physically located in Surrey while Fraser Health Access Line serves Burnaby and the broader region." }, { document_id: "interior-health-access-central", evidence: "Access Central serves many Interior communities by telephone without being a physical facility in each community." }], applicability: "shared", proposed_rule: "Persist physical location, local service area, regional service area, province-wide, virtual and navigation-only semantics separately, and label search results by their relationship to the searched community." }, { now: at }),
]

const promoted = lessons.map(lesson => lesson.proposed_rule ? proposePalantirRulePromotion(lesson, { regressionTest: true, reviewedBy: "owner_authorized_pass" }) : lesson)
const ledger = buildPalantirLearningLedger(promoted, { now: at })
const artifact = { ...ledger, summary: { lessons: ledger.lessons.length, promoted_shared_rules: ledger.lessons.filter(item => item.promoted).length, source_specific: ledger.lessons.filter(item => item.applicability === "source_specific").length, automatic_rule_changes: 0, model_training: false }, regression_coverage: ["structured enforcement row fingerprinting", "index versus full-document roles", "HEAD-to-GET URL health fallback", "regulator case-number keys", "prosecution procedural fields", "human-rights evidence-role separation", "research-document versus verified-resource separation", "canonical-over-legacy resource enrichment", "physical-location versus service-area separation"] }
const output = path.join(root, "artifacts", "samwise-public-records", "palantir-operational-learning-ledger-v1.json")
mkdirSync(path.dirname(output), { recursive: true })
writeFileSync(output, `${JSON.stringify(artifact, null, 2)}\n`)
process.stdout.write(`${JSON.stringify({ output, summary: artifact.summary }, null, 2)}\n`)
