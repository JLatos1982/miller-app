import test from "node:test"
import assert from "node:assert/strict"
import { createMaintenanceCycleStore } from "../server/maintenanceCycleRuns.js"
test("maintenance store refuses an active cycle", async () => { const chain = { select(){ return this }, eq(){ return this }, order(){ return this }, limit(){ return this }, async maybeSingle(){ return { data: { id: "active", started_at: new Date().toISOString() }, error: null } } }; const store = createMaintenanceCycleStore({ from(){ return chain } }); assert.equal((await store.start()).already_running, true) })
test("cycle store exposes stale ownership without silently mutating it", async () => { let updated = false; const chain = { select(){ return this }, eq(){ return this }, order(){ return this }, limit(){ return this }, update(){ updated = true; return this }, async maybeSingle(){ return { data: { id: "stale", status: "running", started_at: "2020-01-01T00:00:00Z" }, error: null } } }; const store = createMaintenanceCycleStore({ from(){ return chain } }); assert.equal((await store.inspectActive()).id, "stale"); assert.equal(updated, false) })
