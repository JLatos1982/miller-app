import test from "node:test";import assert from "node:assert/strict";import {configurationDrift,parseNpmAudit,safeRepositoryPath} from "../server/securityInstruments.js";
test("audit parser is bounded and stable without raw output",()=>{const items=parseNpmAudit({vulnerabilities:{x:{severity:"high",via:[{title:"CVE",severity:"high",url:"https://x"}]}}});assert.equal(items.length,1);assert.equal(items[0].package,"x");assert.doesNotMatch(JSON.stringify(items),/stdout|token/i)});
test("repository paths are relative and reject escapes",()=>{assert.equal(safeRepositoryPath("/repo","/repo/server/x.js"),"server/x.js");assert.throws(()=>safeRepositoryPath("/repo","/other/key"),/denied/)});
test("drift checks report deterministic failures",()=>{const checks=configurationDrift({serverSource:'app.get("/api/admin/control-room",requireAdmin) security-review/persist",requireAdmin confirm!==true scheduling:"not_enabled" unknown_defensive_capability',clientSource:""});assert.equal(checks.every(x=>x.passed),true)});
