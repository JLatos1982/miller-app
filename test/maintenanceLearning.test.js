import test from "node:test";import assert from "node:assert/strict";import { lessonFromOutcome,reinforceLesson,growthOpportunity } from "../server/maintenanceLearning.js";
test("verified outcomes create narrow candidate lessons and contradictions preserve support",()=>{const evidence=lessonFromOutcome({domain:"operations",action_id:"recover_stale_maintenance_cycle",target_type:"maintenance_cycle",verification:"passed",classification:"resolved"});let lesson=reinforceLesson(null,evidence);assert.equal(lesson.status,"candidate");lesson=reinforceLesson(lesson,evidence);lesson=reinforceLesson(lesson,evidence);assert.equal(lesson.status,"supported");lesson=reinforceLesson(lesson,lessonFromOutcome({domain:"operations",action_id:"recover_stale_maintenance_cycle",target_type:"maintenance_cycle",verification:"failed",classification:"failed"}));assert.equal(lesson.support_count,3);assert.equal(lesson.status,"contradicted")});
test("opportunity identity is stable and does not authorize execution",()=>{const item=growthOpportunity({domain:"resource_data",gap_type:"stale_evidence",target_key:"resource:1",reason:"Evidence is old.",priority:80}),again=growthOpportunity({domain:"resource_data",gap_type:"stale_evidence",target_key:"resource:1",reason:"x"});assert.equal(item.opportunity_fingerprint,again.opportunity_fingerprint);assert.equal(item.state,"candidate")});
