import { recheckPriority } from "./maintenance.js"
export function buildFreshnessQueue(facts = [], now = new Date()) { const rank = { urgent: 0, high: 1, normal: 2, low: 3 }; return facts.map((fact) => ({ ...fact, recheck: recheckPriority(fact, now), researchBudget: { maxQueries: fact.previousConflict ? 2 : 1, maxPages: 2, maxElapsedMs: 20_000 } })).sort((a, b) => rank[a.recheck.priority] - rank[b.recheck.priority] || Number(Boolean(b.previousConflict)) - Number(Boolean(a.previousConflict)) || String(a.subjectId).localeCompare(String(b.subjectId))) }
export function millerWatchPlan(facts = [], controls, now = new Date()) { return { name: "Miller Watch", enabled: false, observeOnly: true, evidenceGatheringAllowed: controls.shadowEnabled === true, automatedWritesAllowed: false, queue: buildFreshnessQueue(facts, now), providerLimits: { tavilyQueriesPerClaim: 2, firstPartyPagesPerClaim: 2, concurrentClaims: 2 }, failurePolicy: "preserve_trusted_fact_and_retry_later", auditRequired: true, killSwitchActive: controls.killSwitchActive !== false } }
