import { createHash } from "node:crypto"
export const SECURITY_AUTHORITY = Object.freeze({ automatic:["observe_sanitized_telemetry","evaluate_known_invariants","persist_sanitized_findings","update_recurrence","verify_existing_controls","intrinsic_request_protections","retain_quarantine"], recommendation_only:["pause_worker","disable_optional_integration","modify_runtime_behavior","isolate_component"], human_only:["rotate_credentials","alter_rls","modify_firewall","modify_infrastructure","delete_data","block_network_target","deploy"] })
export const DEFENSE_REGISTRY=Object.freeze({ npm_audit:{id:"npm_audit",tier:0,environment:"local_only",read_only:true,timeout_ms:30000,handler:"dependency_posture"}, security_review:{id:"security_review",tier:0,environment:"local_only",read_only:true,timeout_ms:250,handler:"aggregate_review"} })
const fingerprint=(item)=>createHash("sha256").update([item.finding_key,item.subsystem,item.defensive_result].join("|")).digest("hex")
export function normalizeSecurityFindings(review={}) { return (review.findings||[]).map((item)=>({finding_fingerprint:fingerprint(item),finding_type:item.finding_key,subsystem:item.subsystem,severity:item.severity,confidence:"observed",lifecycle:item.defensive_result==="authorization_denied"?"expected_behavior":"new",description:item.observation,defensive_control:item.defensive_control||null,defensive_result:item.defensive_result,recommended_action:item.recommended_action,evidence_metadata:{aggregate_only:true,raw_request_retained:false}})) }
export async function runDefense({ capabilityId, environment="local", context={} }={}) { const capability=DEFENSE_REGISTRY[capabilityId];if(!capability)throw new Error("unknown_defensive_capability");if(capability.environment!=="local_only"||environment!=="local")throw new Error("defensive_capability_environment_denied");if(context&&Object.keys(context).length)throw new Error("defensive_capability_context_denied");return {capability_id:capability.id,state:"verified_success",tier:capability.tier,external_requests:0,mutations:0} }
